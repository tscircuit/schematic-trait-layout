{
  "name": "@tscircuit/pmars-layout",
  "version": "0.0.1",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "generate-comments": "bun run scripts/generate-template-comments.ts",
    "format": "biome format --write .",
    "format:check": "biome format --check .",
    "website:dev": "cd website && bun install && bun run dev",
    "website:build": "cd website && bun install && bun run build.ts",
    "website:serve": "cd website && bun run server.tsx"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@tscircuit/circuit-json-util": "^0.0.48",
    "@types/bun": "latest",
    "@types/debug": "^4.1.12",
    "bun-match-svg": "^0.0.11",
    "circuit-to-svg": "^0.0.128",
    "debug": "^4.4.1",
    "schematic-symbols": "^0.0.139",
    "tscircuit": "^0.0.470"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@types/glob": "^8.1.0",
    "glob": "^11.0.2"
  }
}
